{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; echo \"$f\" | grep -qE '\\.ts$' && npx tsc --noEmit 2>&1 || true; }",
            "timeout": 30,
            "statusMessage": "Type-checking…"
          }
        ]
      }
    ]
  }
}
